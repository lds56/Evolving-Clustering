package whatever

object Main extends App {
  //val keywordExt = new KeywordExtractor("keyword_extractor_test")
  //println(keywordExt.extract.map(_._1).sum)
  //keywordExt.extract foreach println
  //val kw = keywordExt.extract
  //val a = kw.map(_._2)
  //val sumOfSquare = kw.map(el => el._1 * el._1).sum
  //val a = kw.map {case (rank, name) => (name, rank / math.sqrt(sumOfSquare))}
  //a map(_._1) foreach println
  //FeatureVector(kw).v map (_._2) foreach println
  //val fv = FeatureVector(kw)
  //val f2 = FeatureVector(List((1.0 -> "types")))
  //println(fv cos f2)
}
